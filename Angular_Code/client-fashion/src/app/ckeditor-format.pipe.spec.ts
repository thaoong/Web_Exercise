import { CkeditorFormatPipe } from './ckeditor-format.pipe';

describe('CkeditorFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CkeditorFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
